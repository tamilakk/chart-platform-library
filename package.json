{
  "name": "chart-platform-library",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplatform chart library for React client rendering and backend image export",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter demo dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}